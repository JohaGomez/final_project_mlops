# final_project_mlops

Pipeline de ML con MLflow y CI/CD (GitHub Actions).

## Estructura
Ver carpetas `src/`, `configs/`, `data/`, `models/`, `mlruns/`.

## Pasos rápidos
```bash
make install
make run
make mlflow-ui
